# Services to add

Put each service logo here and add a short text file containing its name, category, description, and official link.

When ready, copy the logo to `static/img/services/` and add the service details to `src/data/services.js`.
