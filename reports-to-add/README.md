# Reports to add

Place each new report PDF here, along with its cover image if you have one. Add a text file with the report title, date, short description, tags, and any slide or web links.

These files are source material only; they are not published automatically. When ready, add the report details to `src/data/reports.js` and copy the final PDF/image into `static/`.
